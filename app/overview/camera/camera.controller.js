SmartHomeManger

    .component('camera', {
        templateUrl: 'overview/camera/camera.html',
        controllerAs: 'cameraController',
        controller: function ($scope, $element, $attrs) {
            var cameraController = this;
            console.log('cameraController');
        }
    })